#!/usr/bin/env python3

import sys
import os

args=sys.argv[]
seqlen = args[2]  #default sequence length
file=args[1] #file input

lines=[]
if not os.path.isfile(file):
 if file.isstring():
  for line in 'CGTA'
  print('{}'.format(seglen))
else:
 for line in open(file):

#core part of code: for characters in string/file print that string up to arg[2]
